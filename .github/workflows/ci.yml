name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('requirements.txt', 'requirements-dev.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python-version }}-

      - name: Install dependencies
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Lint (ruff)
        run: ruff check src/ tests/

      - name: Format check (black)
        run: black --check src/ tests/

      # Integration tests make yfinance network calls and are excluded from CI
      # by pytest.ini (addopts = -m "not integration"), so no -m flag is needed
      # here -- a plain `pytest` run stays fully offline and deterministic.
      - name: Run tests
        run: pytest tests/ -q --tb=short

      # Coverage report. XML is emitted for the codecov upload below;
      # term-missing prints the real coverage and any missing lines in the log.
      - name: Coverage report
        run: pytest tests/ --cov=src --cov-report=xml --cov-report=term-missing -q

      # fail_ci_if_error is false on purpose: a codecov outage must not break
      # CI for the tests themselves, which have already passed at this point.
      - name: Upload coverage to codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          fail_ci_if_error: false
          verbose: false
