[pytest]
# Register the integration marker and deselect it by default so a plain
# `pytest` run stays fully offline. Run network/integration tests explicitly
# with:  pytest -m integration
markers =
    integration: requires network / real market data (deselected by default)
addopts = -m "not integration"
